name: Rebuild claim.db

on:
  push:
    paths:
      - 'tools/claim-db/**'
      - '.github/workflows/claim-db.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Rebuild SQLite claim database
        run: python tools/claim-db/build.py
      - name: Verify database
        run: |
          python - <<'PY'
          import sqlite3
          con = sqlite3.connect('claim.db')
          assert con.execute('PRAGMA integrity_check').fetchone()[0] == 'ok'
          assert con.execute('SELECT COUNT(*) FROM sources').fetchone()[0] >= 1
          assert con.execute('SELECT COUNT(*) FROM claims').fetchone()[0] >= 1
          print('claim.db integrity ok')
          PY
      - name: Commit regenerated database
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add claim.db
          if git diff --cached --quiet; then
            echo 'claim.db already current'
          else
            git commit -m 'Regenerate SHADED claim.db from corpus'
            git push
          fi
